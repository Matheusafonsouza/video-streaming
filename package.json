{
  "name": "meet",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port $PORT",
    "build": "vite build",
    "serve": "vite preview --port $PORT"
  },
  "devDependencies": {
    "vite": "^2.0.5"
  },
  "dependencies": {
    "firebase": "^8.2.10"
  }
}
